#include <bits/stdc++.h>
using namespace std;

int main() {
  // 文字列は配列となり、文字列同士は足せない
  // cout << "string" + "string" << endl;
  string s = "string";
  string t = "sample";

  cout << s + t << endl;
  cout << s[1] << endl;
  //メンバー変数
  cout << s.size() << endl;
}